from kmath:api import Vec3
from bolt_expressions import Scoreboard, Data

def launch_looking(strength):
    arg = Scoreboard.objective("player_motion.api.launch")["$strength"]
    arg = strength

    function player_motion:api/launch_looking

def launch_xyz(vector):
    obj = Scoreboard.objective("player_motion.api.launch")
    mx = obj["$x"]
    my = obj["$y"]
    mz = obj["$z"]

    mx = vector[0]
    my = vector[1]
    mz = vector[2]
    function player_motion:api/launch_xyz